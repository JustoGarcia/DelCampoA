<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Del Campo" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Favicon y manifiesto -->
    <link rel="icon" href="<%= asset_path('favicon-32x32.png') %>" sizes="32x32" type="image/png">
    <link rel="manifest" href="<%= asset_path('site.webmanifest') %>">

    <!-- Hojas de estilo -->
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", type: "module", "data-turbo-track": "reload" %>
    
    <style>
      html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: flex-start; /* Asegura que el contenido se pegue arriba */
        align-items: center; /* Centra horizontalmente */
      }

      .container {
        min-height: 0 !important;
        margin: 0 auto; /* Opcional: para centrar horizontalmente */
        padding: 0; /* Asegúrate de que no haya padding */
        width: 100%; /* Asegura que ocupe todo el ancho disponible */
        text-align: center; /* Centra el contenido */
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
